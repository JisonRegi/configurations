server:
  port: 8001
spring:
  application:
    name: USER-SERVICE
  datasource:
    url: jdbc:postgresql://localhost:5432/userdbTest
    username: ${db.username}
    password: ${db.password}
  jpa:
    hibernate:
     ddl-auto: update
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/